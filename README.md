# Livably

A simple Node.js web app prototype for Livably, a residential address intelligence report for homebuyers.

## Getting started

1. Run `npm install`
2. Run `npm start`
3. Open `http://localhost:3000`

## Current prototype

- `public/index.html`: address input form
- `src/app.js`: basic Express server
