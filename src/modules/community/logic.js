'use strict';

// Stub: getDensityType lives here after Task 10 (community module extraction).
// Property module imports from here now for forward-compatibility.
// Task 10 will move the full implementation here and remove it from data.js.
const { getDensityType } = require('./data');

module.exports = { getDensityType };
